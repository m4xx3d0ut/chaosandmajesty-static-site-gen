apiVersion: v1
kind: Service
metadata: { name: vllm, namespace: ai }
spec:
  selector:
    ai.zlabs.dev/component: vllm
  ports:
    - { name: openai, port: 8000, targetPort: openai }
    - { name: cachectl, port: 8081, targetPort: cachectl }
