# Inventory for RadioStatic production environment
# Override ansible_host via --inventory if you prefer not to commit the VPS address.
[vps]
prod ansible_host=radiostatic.example
ansible_user=deploy
ansible_port=22
ansible_ssh_private_key_file=~/.ssh/gitea-ci-prod
