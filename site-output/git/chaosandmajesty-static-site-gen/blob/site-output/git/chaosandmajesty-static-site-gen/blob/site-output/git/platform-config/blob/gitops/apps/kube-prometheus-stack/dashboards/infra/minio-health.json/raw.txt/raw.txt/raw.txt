{
  "title": "MinIO – Health",
  "timezone": "browser",
  "refresh": "15s",
  "schemaVersion": 38,
  "tags": ["minio","s3"],
  "templating": {
    "list": [
      {
        "name": "bucket",
        "type": "query",
        "datasource": {"type":"prometheus","uid":"mimir"},
        "query": "label_values(minio_bucket_usage_object_total, bucket)",
        "includeAll": true,
        "multi": true,
        "refresh": 1
      }
    ]
  },
  "panels": [
    {
      "type": "stat",
      "title": "Cluster capacity used (%)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "fieldConfig": { "defaults": { "unit": "percent" } },
      "targets": [
        { "expr": "100 * (minio_cluster_capacity_usage_total_bytes / minio_cluster_capacity_usable_total_bytes)" }
      ],
      "gridPos": {"x": 0, "y": 0, "w": 6, "h": 6}
    },
    {
      "type": "stat",
      "title": "Online nodes",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        { "expr": "sum(minio_cluster_nodes_online_total)" }
      ],
      "gridPos": {"x": 6, "y": 0, "w": 6, "h": 6}
    },
    {
      "type": "stat",
      "title": "Total objects",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        { "expr": "sum(minio_bucket_usage_object_total{bucket=~\"$bucket\"})" }
      ],
      "gridPos": {"x": 12, "y": 0, "w": 6, "h": 6}
    },
    {
      "type": "timeseries",
      "title": "Bucket size (bytes)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "fieldConfig": { "defaults": { "unit": "bytes" } },
      "targets": [
        {
          "expr": "sum by (bucket) (minio_bucket_usage_size_bytes{bucket=~\"$bucket\"})",
          "legendFormat": "{{bucket}}"
        }
      ],
      "gridPos": {"x": 0, "y": 6, "w": 12, "h": 8}
    },
    {
      "type": "timeseries",
      "title": "Disk used % (by node)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "fieldConfig": { "defaults": { "unit": "percent" } },
      "targets": [
        {
          "expr": "100 * (minio_node_disk_used_bytes / minio_node_disk_total_bytes)",
          "legendFormat": "{{server}}"
        }
      ],
      "gridPos": {"x": 12, "y": 6, "w": 12, "h": 8}
    },
    {
      "type": "table",
      "title": "Top buckets by objects",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        { "expr": "topk(10, sum by (bucket) (minio_bucket_usage_object_total))" }
      ],
      "gridPos": {"x": 0, "y": 14, "w": 24, "h": 8}
    }
  ]
}

