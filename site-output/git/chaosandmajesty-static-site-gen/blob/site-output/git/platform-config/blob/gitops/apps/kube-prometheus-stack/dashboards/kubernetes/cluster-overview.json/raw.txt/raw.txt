{
  "title": "Kubernetes – Cluster Overview",
  "timezone": "browser",
  "refresh": "10s",
  "schemaVersion": 38,
  "tags": ["kubernetes","overview"],
  "templating": {
    "list": [
      {
        "name": "namespace",
        "type": "query",
        "datasource": {"type":"prometheus","uid":"mimir"},
        "query": "label_values(kube_pod_info, namespace)",
        "hide": 0,
        "refresh": 1,
        "includeAll": true,
        "multi": true,
        "current": {"selected": false}
      },
      {
        "name": "instance",
        "type": "query",
        "datasource": {"type":"prometheus","uid":"mimir"},
        "query": "label_values(node_cpu_seconds_total, instance)",
        "hide": 0,
        "refresh": 1,
        "includeAll": true,
        "multi": true,
        "current": {"selected": false}
      }
    ]
  },
  "panels": [
    {
      "type": "timeseries",
      "title": "CPU usage % (by node)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        {
          "expr": "100 - (avg by (instance) (irate(node_cpu_seconds_total{mode=\"idle\",instance=~\"$instance\"}[$__rate_interval])) * 100)",
          "legendFormat": "{{instance}}"
        }
      ],
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8}
    },
    {
      "type": "timeseries",
      "title": "Memory usage % (by node)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        {
          "expr": "100 * (1 - (avg by (instance) (node_memory_MemAvailable_bytes{instance=~\"$instance\"}) / avg by (instance) (node_memory_MemTotal_bytes{instance=~\"$instance\"})))",
          "legendFormat": "{{instance}}"
        }
      ],
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8}
    },
    {
      "type": "bargauge",
      "title": "Pod restarts (last 10m) by namespace",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "options": {"orientation": "horizontal", "displayMode": "gradient"},
      "targets": [
        {
          "expr": "sum by (namespace) (increase(kube_pod_container_status_restarts_total{namespace=~\"$namespace\"}[10m]))",
          "legendFormat": "{{namespace}}"
        }
      ],
      "gridPos": {"x": 0, "y": 8, "w": 12, "h": 7}
    },
    {
      "type": "stat",
      "title": "Pending pods (count)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        { "expr": "sum(kube_pod_status_phase{phase=\"Pending\", namespace=~\"$namespace\"})" }
      ],
      "gridPos": {"x": 12, "y": 8, "w": 6, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "Filesystem usage % (by node)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        {
          "expr": "100 * (1 - (sum by (instance) (node_filesystem_avail_bytes{fstype!~\"tmpfs|rootfs|overlay|squashfs\", mountpoint!~\"/run|/var/lib/(docker|containerd).*\"}) / sum by (instance) (node_filesystem_size_bytes{fstype!~\"tmpfs|rootfs|overlay|squashfs\", mountpoint!~\"/run|/var/lib/(docker|containerd).*\"})))",
          "legendFormat": "{{instance}}"
        }
      ],
      "gridPos": {"x": 18, "y": 8, "w": 6, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "API server request rate (5xx)",
      "datasource": {"type": "prometheus", "uid": "mimir"},
      "targets": [
        { "expr": "sum(rate(apiserver_request_total{code=~\"5..\"}[$__rate_interval]))", "legendFormat": "5xx req/s" }
      ],
      "gridPos": {"x": 0, "y": 15, "w": 12, "h": 7}
    }
  ]
}

