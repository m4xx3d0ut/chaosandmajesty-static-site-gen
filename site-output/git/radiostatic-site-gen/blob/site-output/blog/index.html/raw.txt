<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RadioStatic Demo – RadioStatic Blog</title>

  <!-- RadioStatic head partial -->

<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=5.0, viewport-fit=cover"/>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=Inter:wght@400;600;700&family=JetBrains+Mono:wght@400;600;700&family=Share+Tech+Mono&display=swap" rel="stylesheet">
<link rel="stylesheet" type="text/css" href="../assets/static/style/terminal-console.css">
<script type="text/javascript" src="../assets/static/js/terminal-console.js" defer></script>
<link rel="manifest" href="../site.webmanifest"/>
<link rel="icon" href="../assets/static/img/site-icon-512.png" type="image/png">
<link rel="apple-touch-icon" sizes="180x180" href="../assets/static/img/site-icon-180.png">


  
  
  
  
  
  
  
  
  
  
  
  
    <link rel="stylesheet" href="../assets/modern.css">
  
  <script src="https://unpkg.com/htmx.org@1.9.12" defer></script>

</head>
<body class="cm-body blog-page">
  <header>
    <div class="container header-container">
      
      
      
        <a href="../index.html" class="logo-link">
          <img src="../assets/static/img/site-icon-512.png" alt="RadioStatic Demo Logo" class="logo">
        </a>
      
      
      
        <button type="button" class="nav-toggle" aria-label="Toggle navigation">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 7h18M3 12h18M3 17h18" />
          </svg>
        </button>
        <nav class="main-nav">
          <ul class="nav-links">
            
              <li><a href="../index.html">Home</a></li>
            
              <li><a href="../blog.html">Blog</a></li>
            
              <li><a href="../sections/git.html">Git</a></li>
            
            <li><button type="button" class="theme-toggle" aria-label="Toggle theme" title="Toggle theme">DARK</button></li>
          </ul>
        </nav>
      
    </div>
  </header>

  <main class="blog-container">
    
    <section class="blog-hero-slab">
      <div class="blog-hero-header">
        
          <figure class="blog-hero-badge">
            <img src="../assets/static/img/site-icon-512.png" alt="RadioStatic Demo signal mark" class="blog-hero-logo">
            <figcaption class="visually-hidden">RadioStatic Demo signal emblem</figcaption>
          </figure>
        
        <div class="blog-hero-copy">
          <h2 class="blog-hero-title">RadioStatic Blog</h2>
          
            <p class="blog-hero-tagline">Dispatches for developers and makers dialing in their personal platform.</p>
          
          
            <p class="blog-hero-intro">Dispatches for developers and makers dialing in their personal platform.</p>
          
        </div>
      </div>
    </section>

    <section class="blog-layout">
      <aside class="blog-sidebar" aria-label="Blog post list" data-page-size="6">
        <h3 class="blog-sidebar-title">Posts</h3>
        
          <div class="blog-author-filter" role="group" aria-label="Filter posts by author">
            <button type="button" class="author-filter-btn is-active" data-author-filter="all">All</button>
            
              <button type="button" class="author-filter-btn" data-author-filter="your-name">Your Name</button>
            
          </div>
        
        <div class="blog-sidebar-search">
          <label class="visually-hidden" for="blog-search-input">Search posts</label>
          <input
            id="blog-search-input"
            type="search"
            class="post-search-input"
            placeholder="Search posts..."
            autocomplete="off"
            spellcheck="false"
          >
        </div>
        <div class="blog-sidebar-pagination is-hidden" aria-label="Post pagination controls">
          <button type="button" class="sidebar-nav-btn" data-sidebar-prev>Prev</button>
          <button type="button" class="sidebar-nav-btn" data-sidebar-next>Next</button>
        </div>
        <ul class="blog-post-list">
          
            <li data-post-item data-author="unknown" data-title="retiring stagit, embracing htmx" data-summary="how the original stagit-powered console evolved into an htmx-driven system with faster refreshes and richer interactions.">
              
              <a
                class="blog-post-link"
                href="../blog/stagit-to-htmx.html"
                data-post-slug="stagit-to-htmx"
                hx-get="../blog/fragments/stagit-to-htmx.html"
                hx-trigger="click"
                hx-target="#blog-article"
                hx-swap="innerHTML"
                hx-push-url="?post=stagit-to-htmx"
                hx-indicator="#blog-loading"
              >
                <h3>Retiring Stagit, Embracing HTMX</h3>
                <div class="blog-post-meta">
                  
                    <span>Oct 20, 2025</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>7 min read</span>
                  
                  
                </div>
                
                  <p>How the original Stagit-powered console evolved into an HTMX-driven system with faster refreshes and richer interactions.</p>
                
              </a>
            </li>
          
            <li data-post-item data-author="your-name" data-title="kickstart your first build" data-summary="follow this quick workflow to move from cloning the repo to your first successful build.">
              
              <a
                class="blog-post-link"
                href="../blog/kickstart-your-first-build.html"
                data-post-slug="kickstart-your-first-build"
                hx-get="../blog/fragments/kickstart-your-first-build.html"
                hx-trigger="click"
                hx-target="#blog-article"
                hx-swap="innerHTML"
                hx-push-url="?post=kickstart-your-first-build"
                hx-indicator="#blog-loading"
              >
                <h3>Kickstart Your First Build</h3>
                <div class="blog-post-meta">
                  
                    <span>Apr 12, 2024</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>4 min read</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>Your Name</span>
                  
                </div>
                
                  <p>Follow this quick workflow to move from cloning the repo to your first successful build.</p>
                
              </a>
            </li>
          
            <li data-post-item data-author="your-name" data-title="launch checklist" data-summary="a short preflight checklist to run before sharing your statically generated site.">
              
              <a
                class="blog-post-link"
                href="../blog/launch-checklist.html"
                data-post-slug="launch-checklist"
                hx-get="../blog/fragments/launch-checklist.html"
                hx-trigger="click"
                hx-target="#blog-article"
                hx-swap="innerHTML"
                hx-push-url="?post=launch-checklist"
                hx-indicator="#blog-loading"
              >
                <h3>Launch Checklist</h3>
                <div class="blog-post-meta">
                  
                    <span>Apr 5, 2024</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>5 min read</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>Your Name</span>
                  
                </div>
                
                  <p>A short preflight checklist to run before sharing your statically generated site.</p>
                
              </a>
            </li>
          
            <li data-post-item data-author="your-name" data-title="styling your pages" data-summary="learn how to plug in your own css and tweak the terminal theme provided with the demo.">
              
              <a
                class="blog-post-link"
                href="../blog/styling-your-pages.html"
                data-post-slug="styling-your-pages"
                hx-get="../blog/fragments/styling-your-pages.html"
                hx-trigger="click"
                hx-target="#blog-article"
                hx-swap="innerHTML"
                hx-push-url="?post=styling-your-pages"
                hx-indicator="#blog-loading"
              >
                <h3>Styling Your Pages</h3>
                <div class="blog-post-meta">
                  
                    <span>Mar 28, 2024</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>3 min read</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>Your Name</span>
                  
                </div>
                
                  <p>Learn how to plug in your own CSS and tweak the terminal theme provided with the demo.</p>
                
              </a>
            </li>
          
            <li data-post-item data-author="your-name" data-title="introducing the demo site" data-summary="tour the example content that ships with radiostatic and see how it maps back to the yaml configuration.">
              
              <a
                class="blog-post-link"
                href="../blog/introducing-the-demo-site.html"
                data-post-slug="introducing-the-demo-site"
                hx-get="../blog/fragments/introducing-the-demo-site.html"
                hx-trigger="click"
                hx-target="#blog-article"
                hx-swap="innerHTML"
                hx-push-url="?post=introducing-the-demo-site"
                hx-indicator="#blog-loading"
              >
                <h3>Introducing the Demo Site</h3>
                <div class="blog-post-meta">
                  
                    <span>Mar 12, 2024</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>4 min read</span>
                  
                  
                    <span aria-hidden="true">•</span>
                    <span>Your Name</span>
                  
                </div>
                
                  <p>Tour the example content that ships with RadioStatic and see how it maps back to the YAML configuration.</p>
                
              </a>
            </li>
          
          
        </ul>
        <div class="blog-sidebar-pagination is-hidden" aria-label="Post pagination controls">
          <button type="button" class="sidebar-nav-btn" data-sidebar-prev>Prev</button>
          <button type="button" class="sidebar-nav-btn" data-sidebar-next>Next</button>
        </div>
      </aside>

      <div class="blog-article-shell">
        <div id="blog-article" aria-live="polite" aria-busy="false" hx-history-elt>
          
            

<article class="blog-article-body" data-slug="welcome-to-radiostatic">
  <header class="blog-article-header">
    <h1 class="blog-article-title">Welcome to RadioStatic</h1>
    <p class="blog-article-meta">
      
        <span>Jan 1, 2024</span>
      
      
        <span aria-hidden="true">•</span>
        <span>2 min read</span>
      
      
        <span aria-hidden="true">•</span>
        <span>Your Name</span>
      
    </p>
  </header>

  
    <figure class="blog-hero">
      <img src="../assets/static/img/hero-blog-card.svg" alt="Welcome to RadioStatic hero image">
    </figure>
  

  <div class="blog-article-content">
    <p>RadioStatic ships with a signal-inspired demo so you can inspect the generated HTML before wiring up your own content. This post is rendered on the home page by default and covers the essentials.</p>
<h2>What this broadcast includes</h2>
<ul>
<li>A homepage with a retro console hero section.</li>
<li>A blog index backed by Markdown files and radio-flavoured copy.</li>
<li>A simulated signal archive (stagit HTML) to showcase code project listings.</li>
</ul>
<h2>Secure next steps</h2>
<p>Update the Markdown files inside <code>content/blog/</code> (or point the generator at your own directory) to replace these examples with your voice. Each file supports front matter fields such as <code>title</code>, <code>author</code>, <code>summary</code>, and <code>tags</code>—check the additional posts for ideas.</p>
<p>When you&#39;re ready, run <code>./build-site.sh</code> with your configuration file to regenerate the output in <code>site-output/</code> and keep the RadioStatic signal fresh.</p>

  </div>

  
    <footer class="blog-author-card">
      
        <img class="blog-author-avatar" src="../assets/static/img/avatar-alex.svg" alt="Your Name avatar">
      
      <div class="blog-author-details">
        <strong class="blog-author-name">Your Name</strong>
        
          <div class="blog-author-title">Signal Keeper</div>
        
        
          <p class="blog-author-bio">Introduce yourself or your crew with a quick sentence about why you broadcast.</p>
        
        
          <ul class="blog-author-social">
            
              <li><a href="https://example.com/you" target="_blank" rel="noopener">@yourhandle</a></li>
            
          </ul>
        
      </div>
    </footer>
  

  
</article>

            <script>
              document.addEventListener('DOMContentLoaded', function() {
                var articleNode = document.querySelector('#blog-article [data-slug]');
                if (articleNode && articleNode.getAttribute('data-slug')) {
                  var activeLink = document.querySelector('[data-post-slug="' + articleNode.getAttribute('data-slug') + '"]');
                  if (activeLink) {
                    activeLink.classList.add('is-active');
                  }
                }
              });
            </script>
          
        </div>
        <div id="blog-loading" class="htmx-indicator" role="status" aria-live="assertive">Loading…</div>
      </div>
    </section>
    <noscript>
      <p>HTMX powers instant loading here. With JavaScript disabled you can still read via the direct links above.</p>
    </noscript>
  </main>

  <footer>
    
      <!-- Terminal footer partial -->
<div class="terminal-footer">
  <p>
    This is placeholder copy. Update cmFooterText in your YAML file.
  </p>
</div>

    
    <div class="container footer-container">
      <p class="footer-copy">&copy; 2025 RadioStatic Demo</p>
    </div>
  </footer>

  <button type="button" class="blog-back-to-top" aria-label="Back to top">
    ▲
  </button>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      var toggleBtn = document.querySelector('.nav-toggle');
      var headerEl = document.querySelector('header');
      if (toggleBtn) {
        toggleBtn.addEventListener('click', function() {
          headerEl.classList.toggle('nav-open');
        });
      }

      var backToTopBtn = document.querySelector('.blog-back-to-top');
      if (backToTopBtn) {
        var updateBackToTop = function() {
          if (typeof window === 'undefined') {
            return;
          }
          var shouldShow = window.scrollY > 320;
          backToTopBtn.classList.toggle('is-visible', shouldShow);
        };
        backToTopBtn.addEventListener('click', function(event) {
          event.preventDefault();
          try {
            window.scrollTo({ top: 0, behavior: 'smooth' });
          } catch (error) {
            window.scrollTo(0, 0);
          }
        });
        updateBackToTop();
        window.addEventListener('scroll', updateBackToTop, { passive: true });
      }
    });

    var filteredListItems = [];
    var currentSlug = null;

    function syncActiveLink(slug) {
      currentSlug = slug || null;
      document.querySelectorAll('.blog-post-link').forEach(function(link) {
        if (slug && link.dataset.postSlug === slug) {
          link.classList.add('is-active');
        } else {
          link.classList.remove('is-active');
        }
      });
      updateArticleNav();
    }

    function scrollArticleToTop() {
      var shell = document.querySelector('.blog-article-shell');
      if (shell && shell.scrollIntoView) {
        try {
          shell.scrollIntoView({ behavior: 'smooth', block: 'start' });
          return;
        } catch (error) {
          // continue to fallback if smooth scrolling is unsupported
        }
        shell.scrollIntoView(true);
        return;
      }
      if (typeof window !== 'undefined') {
        try {
          window.scrollTo({ top: 0, behavior: 'smooth' });
        } catch (error) {
          window.scrollTo(0, 0);
        }
      }
    }

    var sidebar = document.querySelector('.blog-sidebar');
    var filterButtons = Array.from(document.querySelectorAll('.author-filter-btn'));
    var searchInput = document.querySelector('.post-search-input');
    var postItems = Array.from(document.querySelectorAll('[data-post-item]'));
    var paginations = Array.from(document.querySelectorAll('.blog-sidebar-pagination'));
    var prevButtons = [];
    var nextButtons = [];
    paginations.forEach(function(pagination) {
      var prev = pagination.querySelector('[data-sidebar-prev]');
      var next = pagination.querySelector('[data-sidebar-next]');
      if (prev) {
        prevButtons.push(prev);
      }
      if (next) {
        nextButtons.push(next);
      }
    });
    var pageSize = sidebar ? parseInt(sidebar.dataset.pageSize || '6', 10) || 6 : 6;
    var currentAuthor = 'all';
    var currentSearch = '';
    var currentPage = 0;

    function normaliseText(value) {
      return (value || '').toString().toLowerCase();
    }

    function itemMatchesFilters(item) {
      var isHidden = item.dataset.hidden === 'true';
      var title = item.dataset.title || normaliseText(item.querySelector('h3') && item.querySelector('h3').textContent);
      var summary = item.dataset.summary || normaliseText(item.querySelector('p') && item.querySelector('p').textContent);
      var searchMatch = !currentSearch || title.indexOf(currentSearch) !== -1 || summary.indexOf(currentSearch) !== -1;

      if (isHidden) {
        return currentSearch && searchMatch;
      }

      if (!searchMatch) {
        return false;
      }

      var authorMatch = currentAuthor === 'all' || item.dataset.author === currentAuthor;
      return authorMatch;
    }

    function updatePaginationControls(totalPages, visibleCount) {
      if (!paginations.length) return;
      paginations.forEach(function(pagination) {
        if (visibleCount <= pageSize) {
          pagination.classList.add('is-hidden');
        } else {
          pagination.classList.remove('is-hidden');
        }
        var prevBtn = pagination.querySelector('[data-sidebar-prev]');
        var nextBtn = pagination.querySelector('[data-sidebar-next]');
        if (prevBtn) {
          prevBtn.disabled = currentPage <= 0;
        }
        if (nextBtn) {
          nextBtn.disabled = currentPage >= totalPages - 1;
        }
      });
    }

    function buildNavNeighborFromItem(item) {
      if (!item) return null;
      var link = item.querySelector('.blog-post-link');
      if (!link) return null;
      var titleNode = link.querySelector('h3');
      var titleText = '';
      if (titleNode && titleNode.textContent) {
        titleText = titleNode.textContent.trim();
      } else if (link.textContent) {
        titleText = link.textContent.trim();
      }
      return {
        slug: link.dataset.postSlug || '',
        title: titleText,
        href: link.getAttribute('href') || '#',
        fragmentHref: link.getAttribute('hx-get') || link.dataset.fragmentHref || ''
      };
    }

    function setNavLink(link, neighbor, direction) {
      if (!link) return;
      var fallbackTitle = direction === 'previous' ? 'No newer posts' : 'No older posts';
      var titleEl = link.querySelector('.blog-article-nav-title');

      if (!neighbor || !neighbor.slug) {
        link.classList.add('is-disabled');
        link.setAttribute('aria-disabled', 'true');
        link.setAttribute('tabindex', '-1');
        ['hx-get', 'hx-target', 'hx-swap', 'hx-push-url', 'hx-indicator'].forEach(function(attr) {
          link.removeAttribute(attr);
        });
        link.dataset.targetSlug = '';
        link.dataset.postSlug = '';
        link.dataset.fragmentHref = '';
        link.setAttribute('data-fragment-href', '');
        link.setAttribute('href', '#');
        if (titleEl) {
          titleEl.textContent = fallbackTitle;
        }
        return;
      }

      link.classList.remove('is-disabled');
      link.removeAttribute('aria-disabled');
      link.removeAttribute('tabindex');
      link.dataset.targetSlug = neighbor.slug;
      link.dataset.postSlug = neighbor.slug;
      link.dataset.fragmentHref = neighbor.fragmentHref || '';
      link.setAttribute('data-fragment-href', neighbor.fragmentHref || '');
      link.setAttribute('href', neighbor.href || '#');
      if (neighbor.fragmentHref) {
        link.setAttribute('hx-get', neighbor.fragmentHref);
      } else {
        link.removeAttribute('hx-get');
      }
      link.setAttribute('hx-target', '#blog-article');
      link.setAttribute('hx-swap', 'innerHTML');
      link.setAttribute('hx-push-url', '?post=' + neighbor.slug);
      link.setAttribute('hx-indicator', '#blog-loading');
      if (titleEl) {
        titleEl.textContent = neighbor.title || fallbackTitle;
      }
    }

    function updateArticleNav() {
      var articleContainer = document.getElementById('blog-article');
      if (!articleContainer) {
        return;
      }

      var articleNode = articleContainer.querySelector('[data-slug]');
      if (articleNode && !currentSlug) {
        currentSlug = articleNode.getAttribute('data-slug');
      }
      if (!articleNode) {
        currentSlug = null;
      }

      var prevLink = articleContainer.querySelector('[data-blog-nav="previous"]');
      var nextLink = articleContainer.querySelector('[data-blog-nav="next"]');
      if (!prevLink && !nextLink) {
        return;
      }

      var hasActiveFilter = currentAuthor !== 'all' || Boolean(currentSearch);
      var candidateItems = filteredListItems.length || hasActiveFilter ? filteredListItems : postItems;
      if (!currentSlug) {
        setNavLink(prevLink, null, 'previous');
        setNavLink(nextLink, null, 'next');
        return;
      }

      var currentIndex = -1;
      for (var i = 0; i < candidateItems.length; i += 1) {
        var item = candidateItems[i];
        if (!item) continue;
        var link = item.querySelector('.blog-post-link');
        if (link && link.dataset.postSlug === currentSlug) {
          currentIndex = i;
          break;
        }
      }

      if (currentIndex === -1) {
        setNavLink(prevLink, null, 'previous');
        setNavLink(nextLink, null, 'next');
        return;
      }

      var prevNeighbor = null;
      for (var j = currentIndex - 1; j >= 0; j -= 1) {
        prevNeighbor = buildNavNeighborFromItem(candidateItems[j]);
        if (prevNeighbor) break;
      }

      var nextNeighbor = null;
      for (var k = currentIndex + 1; k < candidateItems.length; k += 1) {
        nextNeighbor = buildNavNeighborFromItem(candidateItems[k]);
        if (nextNeighbor) break;
      }

      setNavLink(prevLink, prevNeighbor, 'previous');
      setNavLink(nextLink, nextNeighbor, 'next');
    }

    function applyFilters() {
      var filteredItems = [];
      postItems.forEach(function(item) {
        var matches = itemMatchesFilters(item);
        var isHidden = item.dataset.hidden === 'true';
        item.classList.toggle('is-hidden-filter', !matches);
        item.classList.toggle('is-search-visible', matches && isHidden);
        item.classList.remove('is-hidden-page');
        if (matches) {
          filteredItems.push(item);
        }
      });

      filteredListItems = filteredItems.slice();

      var totalPages = Math.max(1, Math.ceil(filteredItems.length / pageSize));
      if (currentPage >= totalPages) {
        currentPage = totalPages - 1;
      }
      var start = currentPage * pageSize;
      var end = start + pageSize;

      filteredItems.forEach(function(item, idx) {
        var inRange = idx >= start && idx < end;
        item.classList.toggle('is-hidden-page', !inRange);
      });

      updatePaginationControls(totalPages, filteredItems.length);
      updateArticleNav();
    }

    if (filterButtons.length) {
      filterButtons.forEach(function(btn) {
        btn.addEventListener('click', function() {
          var authorId = btn.dataset.authorFilter;
          currentAuthor = authorId || 'all';
          currentPage = 0;
          filterButtons.forEach(function(b) {
            b.classList.toggle('is-active', b === btn);
          });
          applyFilters();
        });
      });
    }

    if (searchInput) {
      searchInput.addEventListener('input', function(event) {
        currentSearch = normaliseText(event.target.value).trim();
        currentPage = 0;
        applyFilters();
      });
    }

    if (prevButtons.length) {
      prevButtons.forEach(function(btn) {
        btn.addEventListener('click', function() {
          if (currentPage > 0) {
            currentPage -= 1;
            applyFilters();
          }
        });
      });
    }

    if (nextButtons.length) {
      nextButtons.forEach(function(btn) {
        btn.addEventListener('click', function() {
          currentPage += 1;
          applyFilters();
        });
      });
    }

    applyFilters();

    var params = new URLSearchParams(window.location.search || '');
    var initialSlug = params.get('post');
    var articleEl = document.getElementById('blog-article');

    if (initialSlug && window.htmx && articleEl) {
      var initialLink = document.querySelector('[data-post-slug="' + initialSlug + '"]');
      if (initialLink && initialLink.getAttribute('hx-get')) {
        syncActiveLink(initialSlug);
        htmx.ajax('GET', initialLink.getAttribute('hx-get'), {
          target: '#blog-article',
          swap: 'innerHTML',
          history: false
        });
      }
    } else {
      var initialArticle = articleEl && articleEl.querySelector('[data-slug]');
      if (initialArticle) {
        syncActiveLink(initialArticle.getAttribute('data-slug'));
      }
    }

    document.addEventListener('htmx:beforeRequest', function(event) {
      var trigger = event.detail && event.detail.elt;
      if (!trigger || !trigger.matches || (!trigger.matches('.blog-post-link') && !trigger.matches('.blog-article-nav-link'))) {
        return;
      }
      syncActiveLink(trigger.dataset.postSlug);
      var article = document.getElementById('blog-article');
      if (article) {
        article.setAttribute('aria-busy', 'true');
      }
    });

    document.addEventListener('htmx:afterSwap', function(event) {
      if (event.target && event.target.id === 'blog-article') {
        var articleNode = event.target.querySelector('[data-slug]');
        if (articleNode) {
          syncActiveLink(articleNode.getAttribute('data-slug'));
        }
        event.target.setAttribute('aria-busy', 'false');
        scrollArticleToTop();
      }
    });

    document.addEventListener('htmx:historyRestore', function() {
      var articleNode = document.querySelector('#blog-article [data-slug]');
      if (articleNode) {
        syncActiveLink(articleNode.getAttribute('data-slug'));
      } else {
        syncActiveLink(null);
      }
      var article = document.getElementById('blog-article');
      if (article) {
        article.setAttribute('aria-busy', 'false');
      }
    });
  </script>
</body>
</html>
