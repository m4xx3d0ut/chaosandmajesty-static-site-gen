# Private CA on MicroK8s (Flux GitOps)

**cert-manager + trust-manager + CA Portal**
*Final state: “Secure mode” (HTTPS + HSTS) with a clean base/overlays layout*

## 0) What you get

* **Local Root CA** (cert-manager) and **ClusterIssuer** for internal TLS
* **trust-manager** bundle that fans the public root into labeled namespaces
* **CA Portal** website to download the CA in **PEM** and **DER**, with install steps
* **Overlays:** “bootstrap” (HTTP, no HSTS) and “secure” (HTTPS, HSTS). We run Secure in prod.

---

## 1) Prereqs

* MicroK8s with **NGINX Ingress** enabled
* Flux bootstrapped
* DNS for portal (e.g., `ca.core.home.arpa`) → Ingress address
* Repo layout under `gitops/` (examples below)

---

## 2) Repository layout (final, cycle-free)

```
gitops/
└─ apps/
   └─ ca-portal/
      ├─ base/
      │  ├─ deploy.yaml
      │  ├─ ingress.yaml         # HTTP-only; overlays patch TLS/HSTS
      │  ├─ kustomization.yaml   # generates ConfigMap from site/index.html
      │  ├─ namespace.yaml
      │  └─ site/index.html
      └─ overlays/
         ├─ bootstrap/
         │  └─ kustomization.yaml   # references ../../base (no TLS)
         └─ secure/
            ├─ certificate.yaml     # cert-manager Certificate (org-ca)
            ├─ kustomization.yaml   # references ../../base + patches
            └─ patch-ingress-secure.yaml
└─ infra/
   ├─ cert-manager/      # ns, HelmRelease, CA bootstrap (root + ClusterIssuer)
   ├─ trust-manager/     # HelmRelease (install/upgrade CRDs = Create)
   └─ trust-bundle/      # Bundle CR in separate Kustomization (no race)
└─ clusters/microk8s-prod/kustomizations/
   ├─ infra-cert-manager.yaml
   ├─ infra-trust-manager.yaml
   ├─ infra-trust-bundle.yaml   # dependsOn: infra-trust-manager
   └─ apps-ca-portal.yaml       # path -> ./gitops/apps/ca-portal/overlays/secure
```

> **Why this shape?** Keeping overlays **outside** the base prevents Kustomize parent/child cycles. Overlays point to `../../base`.

---

## 3) Helm repo (Jetstack)

`gitops/clusters/microk8s-prod/infra/core/helmrepo-jetstack.yaml`

```yaml
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: jetstack
  namespace: flux-system
spec:
  interval: 1h
  url: https://charts.jetstack.io
```

---

## 4) cert-manager (app + CA bootstrap)

`gitops/infra/cert-manager/namespace.yaml`

```yaml
apiVersion: v1
kind: Namespace
metadata: { name: cert-manager }
```

`gitops/infra/cert-manager/release.yaml`

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 10m
  chart:
    spec:
      chart: cert-manager
      version: v1.15.3
      sourceRef: { kind: HelmRepository, name: jetstack, namespace: flux-system }
  install: { createNamespace: true }
  values: { installCRDs: true }
```

`gitops/infra/cert-manager/ca-bootstrap.yaml`

```yaml
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: bootstrap-selfsigned }
spec: { selfSigned: {} }
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: org-root-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: "Platform Internal Root CA"
  secretName: org-root-ca-secret
  duration: 87600h
  renewBefore: 720h
  privateKey: { algorithm: RSA, size: 4096 }
  issuerRef: { name: bootstrap-selfsigned, kind: ClusterIssuer }
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: org-ca }
spec:
  ca: { secretName: org-root-ca-secret }
```

`gitops/infra/cert-manager/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - release.yaml
  - ca-bootstrap.yaml
```

> Production hardening: keep root CA offline and use an in-cluster **intermediate**; clients still trust the root.

---

## 5) trust-manager (chart) + trust-bundle (CR)

**Split** chart and Bundle to avoid CRD race.

`gitops/infra/trust-manager/release.yaml`

```yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trust-manager
  namespace: cert-manager
spec:
  interval: 10m
  install: { crds: Create }
  upgrade: { crds: Create }
  chart:
    spec:
      chart: trust-manager
      version: v0.10.0
      sourceRef: { kind: HelmRepository, name: jetstack, namespace: flux-system }
```

`gitops/infra/trust-manager/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [ release.yaml ]
```

`gitops/infra/trust-bundle/bundle.yaml`

```yaml
apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: org-root-bundle
  namespace: cert-manager
spec:
  sources:
    - secret:
        name: org-root-ca-secret
        key: tls.crt      # no namespace field here
  target:
    configMap: { key: ca.crt }
    namespaceSelector:
      matchLabels: { trust-bundle: "true" }
```

`gitops/infra/trust-bundle/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [ bundle.yaml ]
```

---

## 6) CA Portal (base)

`gitops/apps/ca-portal/base/namespace.yaml`

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: ca-portal
  labels: { trust-bundle: "true" }
```

`gitops/apps/ca-portal/base/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deploy.yaml
  - ingress.yaml
configMapGenerator:
  - name: ca-portal-site
    namespace: ca-portal
    files:
      - index.html=site/index.html
generatorOptions:
  disableNameSuffixHash: true
```

`gitops/apps/ca-portal/base/deploy.yaml`

```yaml
apiVersion: apps/v1
kind: Deployment
metadata: { name: ca-portal, namespace: ca-portal }
spec:
  replicas: 1
  selector: { matchLabels: { app: ca-portal } }
  template:
    metadata: { labels: { app: ca-portal } }
    spec:
      initContainers:
        - name: make-der
          image: alpine:3.20
          command: ["/bin/sh","-c"]
          args:
            - |
              set -euo pipefail
              apk add --no-cache openssl
              cp /ca/ca.crt /work/org-root-ca.crt
              openssl x509 -outform der -in /ca/ca.crt -out /work/org-root-ca.der
          volumeMounts:
            - { name: ca-bundle, mountPath: /ca }
            - { name: downloads, mountPath: /work }
      containers:
        - name: nginx
          image: ghcr.io/nginxinc/nginx-unprivileged:alpine
          ports: [ { containerPort: 8080 } ]
          volumeMounts:
            - { name: site, mountPath: /usr/share/nginx/html }
            - { name: downloads, mountPath: /usr/share/nginx/html/downloads }
      volumes:
        - name: site
          configMap: { name: ca-portal-site }
        - name: downloads
          emptyDir: {}
        - name: ca-bundle
          configMap:
            name: org-root-bundle
            items: [ { key: ca.crt, path: ca.crt } ]
---
apiVersion: v1
kind: Service
metadata: { name: ca-portal, namespace: ca-portal }
spec:
  selector: { app: ca-portal }
  ports: [ { port: 80, targetPort: 8080 } ]
```

`gitops/apps/ca-portal/base/ingress.yaml` (HTTP; overlays patch it)

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ca-portal
  namespace: ca-portal
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    nginx.ingress.kubernetes.io/hsts: "false"
    nginx.ingress.kubernetes.io/hsts-include-subdomains: "false"
    nginx.ingress.kubernetes.io/hsts-preload: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: ca.core.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: { name: ca-portal, port: { number: 80 } }
```

`gitops/apps/ca-portal/base/site/index.html`
*(short page with download links + install steps and a placeholder `SHA256: <fingerprint>` line)*

---

## 7) CA Portal overlays

**Bootstrap (HTTP only)**
`gitops/apps/ca-portal/overlays/bootstrap/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [ ../../base ]
# Defensive removal if tls ever sneaks in
patches:
  - target: { group: networking.k8s.io, version: v1, kind: Ingress, name: ca-portal, namespace: ca-portal }
    patch: |
      - op: remove
        path: /spec/tls
```

**Secure (HTTPS + HSTS)**
`gitops/apps/ca-portal/overlays/secure/certificate.yaml`

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata: { name: ca-portal-tls, namespace: ca-portal }
spec:
  secretName: ca-portal-tls
  dnsNames: [ "ca.core.home.arpa" ]
  issuerRef: { name: org-ca, kind: ClusterIssuer }
```

`gitops/apps/ca-portal/overlays/secure/patch-ingress-secure.yaml`

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ca-portal
  namespace: ca-portal
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/hsts: "true"
    nginx.ingress.kubernetes.io/hsts-include-subdomains: "false"
    nginx.ingress.kubernetes.io/hsts-preload: "false"
spec:
  tls:
    - hosts: [ "ca.core.home.arpa" ]
      secretName: ca-portal-tls
```

`gitops/apps/ca-portal/overlays/secure/kustomization.yaml`

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - certificate.yaml
patchesStrategicMerge:
  - patch-ingress-secure.yaml
```

---

## 8) Flux Kustomizations (cluster level)

**Order matters**: cert-manager → trust-manager → trust-bundle → apps

* `infra-cert-manager` → `./gitops/infra/cert-manager`
* `infra-trust-manager` → `./gitops/infra/trust-manager` (depends on cert-manager)
* `infra-trust-bundle` → `./gitops/infra/trust-bundle` (**depends on trust-manager**)
* `apps-ca-portal` → `./gitops/apps/ca-portal/overlays/secure` (**depends on trust-bundle**)

---

## 9) Fingerprint step (one time)

After `org-root-ca-secret` is Ready:

```bash
kubectl -n cert-manager get secret org-root-ca-secret -o jsonpath='{.data.tls\.crt}' | base64 -d > org-root-ca.crt
openssl x509 -noout -fingerprint -sha256 -in org-root-ca.crt
```

Paste the **SHA256** into the portal’s `index.html`. Commit & push.

---

## 10) Issue leaf certs (example: Grafana)

`gitops/apps/kube-prometheus-stack/certificate-grafana.yaml`

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata: { name: grafana-tls, namespace: monitoring }
spec:
  secretName: grafana-tls
  dnsNames: [ "grafana.core.home.arpa" ]
  issuerRef: { name: org-ca, kind: ClusterIssuer }
```

`ingress.spec.tls[].secretName = grafana-tls`

---

## 11) Verify

```bash
# Flux objects
flux get kustomizations -A

# cert-manager + CA
kubectl -n cert-manager get pods,certificate,clusterissuer
kubectl get crd bundles.trust.cert-manager.io

# trust bundle projected
kubectl -n ca-portal get configmap org-root-bundle -o yaml | sed -n '1,40p'

# Portal (secure)
curl -I https://ca.core.home.arpa/ | grep -i strict-transport-security

# Leaf matches secret
echo | openssl s_client -connect grafana.core.home.arpa:443 -servername grafana.core.home.arpa -showcerts 2>/dev/null \
  | openssl x509 -noout -subject -issuer -fingerprint -sha256
kubectl -n monitoring get secret grafana-tls -o jsonpath='{.data.tls\.crt}' | base64 -d >/tmp/grafana.crt
openssl x509 -in /tmp/grafana.crt -noout -subject -issuer -fingerprint -sha256
```

---

## 12) Common pitfalls (and fixes)

* **Kustomize cycle**: overlays must be **sibling** of base. In overlays’ `kustomization.yaml`, reference **`../../base`** (not `../` nor `../../` pointing at the folder that contains overlays).
* **Bundle schema**: don’t put `namespace:` under `spec.sources.secret` for trust-manager v0.10.0.
* **HSTS gotcha**: if you ever served the portal with HSTS, Chrome can force HTTPS while in bootstrap. Clear `chrome://net-internals/#hsts` for the host, or use the **bootstrap overlay** hostname temporarily.
* **Nginx “Fake Certificate”**: means the Ingress secret isn’t referenced/ready. Ensure `certificate` Ready, `secret` present, `ingress.spec.tls[].secretName` correct. If needed, `rollout restart deploy/ingress-nginx-controller`.

---

## 13) Cleanup

Remove app folders and Flux Kustomizations; Flux will prune:

```bash
git rm -r gitops/apps/ca-portal gitops/infra/trust-bundle gitops/infra/trust-manager gitops/infra/cert-manager
# and their cluster kustomizations
git commit -m "cleanup: remove private CA stack"
git push
```

---

### Notes

* The portal only ever mounts the **public** cert via ConfigMap; it never sees the private key.
* For fleets, consider enabling Chrome/Chromium **ImportEnterpriseRoots** policy or importing your CA into NSS with `certutil`.
* Prefer an **offline root + in-cluster intermediate** for long-term ops.

---

# Extended: App TLS Setup & Troubleshooting

## A) App TLS wiring pattern (the “one file per app” rule)

**Goal:** each app/hostname has a `Certificate` in the **same namespace as the app/ingress**, and the Ingress `tls.secretName` points to it. TLS terminates at ingress-nginx.

**Template**

```
gitops/apps/<app>/
  ├─ kustomization.yaml         # includes release/manifests + certificate
  ├─ release.yaml               # (if HelmRelease)
  ├─ ingress.yaml               # has tls[].secretName that matches the Certificate
  └─ certificate-<app>.yaml     # cert-manager Certificate for the host
```

**Certificate (generic)**

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: <app>-tls
  namespace: <app-namespace>
spec:
  secretName: <app>-tls
  dnsNames: ["<host.example.local>"]
  issuerRef: { name: org-ca, kind: ClusterIssuer }
  privateKey: { algorithm: RSA, size: 2048 }
  # duration/renewBefore optional; cert-manager sensible defaults are fine
```

**Ingress (generic)**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: <app>
  namespace: <app-namespace>
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx          # be explicit; avoids multi-controller surprises
  tls:
    - secretName: <app>-tls
      hosts: ["<host.example.local>"]   # must EXACTLY match rule host
  rules:
    - host: <host.example.local>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: <svc>, port: { number: 80 } } }
```

**Kustomization (generic)**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml        # if HelmRelease is used
  - ingress.yaml
  - certificate-<app>.yaml
```

**Namespace needs the trust bundle only if pods validate internal TLS**

```bash
kubectl label namespace <app-namespace> trust-bundle=true --overwrite
```

Mount/use `org-root-bundle` only when workloads must trust *other* internal services. Ingress itself does **not** need this.

---

## B) Quick verify (copy/paste friendly)

```bash
# 1) Flux objects
flux get kustomizations -A

# 2) Certs + issuer health
kubectl -n cert-manager get pods
kubectl get clusterissuer org-ca -o wide
kubectl -A get certificate

# 3) App’s cert + secret present?
kubectl -n <ns> get certificate <app>-tls -o wide
kubectl -n <ns> get secret <app>-tls

# 4) Ingress wiring is exact?
kubectl -n <ns> get ingress <app> -o yaml | yq '.spec | {ingressClassName, tls, rules}'

# 5) What cert is actually served (SNI):
openssl s_client -connect <host>:443 -servername <host> -showcerts </dev/null \
 | openssl x509 -noout -subject -issuer -fingerprint -sha256

# 6) Client path test (no -k if client trusts your org CA):
curl -I https://<host>/
```

> Heads-up: Ingress cert/secret updates do **not** roll your app Pods; nginx hot-reloads the secret.

---

## C) Troubleshooting matrix (symptom → cause → fix)

### 1) Browser/`openssl` shows **“Kubernetes Ingress Controller Fake Certificate”**

**Cause:** nginx is not using your TLS secret (Ingress not admitted or host/secret mismatch).
**Fix:**

```bash
# bind to the right controller
kubectl -n <ns> patch ingress <app> --type=merge -p '{"spec":{"ingressClassName":"nginx"}}'

# ensure TLS hosts EXACTLY match the rule host
kubectl -n <ns> patch ingress <app> --type=json \
  -p='[{"op":"replace","path":"/spec/tls/0/hosts","value":["<host>"]}]'

# nudge reload if needed
kubectl -n ingress-nginx rollout restart deploy/ingress-nginx-controller
```

### 2) `curl` works with `-k` but fails without it as “self-signed”

**Cause (A):** client doesn’t trust your org root. Install the CA.
**Cause (B):** nginx is serving its default self-signed (see #1).
**Fix:** For (A) import CA; for (B) fix Ingress admission/hosts/secret as above.

### 3) `Certificate` stuck `READY=False` or no Secret created

**Cause:** issuer not Ready, approver/controller stuck, or CSR sign error.
**Fix:**

```bash
# describe shows exact reason
kubectl -n <ns> describe certificate <app>-tls
kubectl -n <ns> get certificaterequests \
  -l cert-manager.io/certificate-name=<app>-tls
kubectl -n <ns> describe certificaterequest <name>

# issuer + CA secret healthy?
kubectl get clusterissuer org-ca -o yaml | sed -n '1,120p'
kubectl -n cert-manager get secret org-root-ca-secret

# restart cert-manager if controllers look wedged
kubectl -n cert-manager logs deploy/cert-manager --tail=200
```

### 4) `Certificate` Ready but Secret looks wrong or nginx ignores it

**Cause:** stale/invalid secret data.
**Fix:** delete secret + force re-issue (safe; pods won’t roll):

```bash
kubectl -n <ns> delete secret <app>-tls --ignore-not-found
kubectl -n <ns> annotate certificate <app>-tls \
  cert-manager.io/renewal-reason=force-rotate-$(date +%s) --overwrite
kubectl -n <ns> get certificate <app>-tls -w
```

### 5) Multiple Ingress controllers present

**Cause:** wrong controller picking (no default class).
**Fix:** set `spec.ingressClassName: nginx` on every Ingress and verify DNS points to the nginx controller Service IP.

### 6) Hostname mismatch between `rules[].host` and `tls[].hosts[]`

**Cause:** SNI mismatch → default cert served.
**Fix:** make them **identical** (string-equal).

### 7) HSTS forces HTTPS during bootstrap

**Cause:** browser cached HSTS from earlier.
**Fix:** use the HTTP-only bootstrap overlay or clear HSTS (`chrome://net-internals/#hsts`).

---

## D) Operations & hygiene

* **Names & shape:** `certificate-<app>.yaml`, secret `=<app>-tls`, keep per-app in app namespace.
* **Rotation:** instant re-issue:

  ```bash
  kubectl -n <ns> annotate certificate <app>-tls cert-manager.io/renewal-reason=force-$(date +%s) --overwrite
  ```
* **Secret recreation:** delete the TLS secret to have cert-manager recreate it (safe for Ingress).
* **No pod restarts:** Ingress terminates TLS; app pods unaffected by cert renewals.
* **Durations:** leave defaults or set `duration/renewBefore` per app; central policy via Kustomize patches if desired.
* **Trust bundle:** label namespaces that need to *trust* your internal root:
  `kubectl label ns <ns> trust-bundle=true --overwrite`
* **Flux ordering:** `cert-manager` → `trust-manager` → `trust-bundle` → **apps** (use `dependsOn` in cluster Kustomizations).

---

## E) Worked example: Gitea

```
gitops/apps/gitea/
├─ kustomization.yaml
├─ release.yaml
└─ certificate-gitea.yaml
```

**certificate-gitea.yaml**

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea-core-tls
  namespace: git-local
spec:
  secretName: gitea-core-tls
  dnsNames: ["gitea.core.home.arpa"]
  issuerRef: { name: org-ca, kind: ClusterIssuer }
  privateKey: { algorithm: RSA, size: 2048 }
```

**ingress (values or manifest)**

```yaml
spec:
  ingressClassName: nginx
  tls:
    - secretName: gitea-core-tls
      hosts: [ "gitea.core.home.arpa" ]
```

**verify**

```bash
kubectl -n git-local get certificate gitea-core-tls -o wide
kubectl -n git-local get secret gitea-core-tls
curl -I https://gitea.core.home.arpa/
```

---

## F) “Why am I still seeing the fake cert?” (90-second checklist)

1. `kubectl -n <ns> get secret <app>-tls` → exists and `type=kubernetes.io/tls`?
2. `kubectl -n <ns> get ingress <app> -o yaml` → `ingressClassName: nginx` and **exact host match** under `tls.hosts` + `rules.host`?
3. `kubectl get ingressclass` → do you have a default class? If not, step 2 is mandatory.
4. `kubectl -n ingress-nginx logs deploy/ingress-nginx-controller --tail=200` → any “secret not found/invalid” lines?
5. Restart controller once to force reload:

   ```bash
   kubectl -n ingress-nginx rollout restart deploy/ingress-nginx-controller
   ```

---

### Bonus: quick “SNI vs Secret” diff helper

```bash
HOST=<host.example.local>; NS=<ns>; SEC=<app>-tls
echo "== served cert =="
openssl s_client -connect ${HOST}:443 -servername ${HOST} </dev/null 2>/dev/null \
 | openssl x509 -noout -subject -issuer -fingerprint -sha256
echo "== secret cert =="
kubectl -n ${NS} get secret ${SEC} -o jsonpath='{.data.tls\.crt}' \
 | base64 -d | openssl x509 -noout -subject -issuer -fingerprint -sha256
```

If both fingerprints match, nginx is serving your secret. If not, fix the Ingress binding/hosts.

---

