apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: local-oci-charts
  namespace: flux-system
spec:
  interval: 10m
  type: oci
  url: oci://reg.microk8s.home.arpa/charts
  # If your registry uses a private CA, add:
  # certSecretRef:
  #   name: local-registry-ca
