apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cm
  namespace: web
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cm.core.home.arpa
      secretName: cm-tls
  rules:
    - host: cm.core.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cm
                port:
                  number: 80
