apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

configMapGenerator:
  - name: kubernetes-cluster-overview
    files:
      - kubernetes/cluster-overview.json
    options:
      disableNameSuffixHash: true

  - name: minio-health
    files:
      - infra/minio-health.json
    options:
      disableNameSuffixHash: true

  - name: loki-operations
    files:
      - logs/loki-operations.json
    options:
      disableNameSuffixHash: true

  - name: service-red
    files:
      - traces/service-red.json
    options:
      disableNameSuffixHash: true

patches:
  - target: { kind: ConfigMap, name: kubernetes-cluster-overview }
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: kubernetes-cluster-overview
        labels: { grafana_dashboard: "1" }
        annotations: { grafana_folder: Kubernetes }
  - target: { kind: ConfigMap, name: service-red }
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: service-red
        labels: { grafana_dashboard: "1" }
        annotations: { grafana_folder: Traces }
  - target: { kind: ConfigMap, name: minio-health }
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: minio-health
        labels: { grafana_dashboard: "1" }
        annotations: { grafana_folder: Infra }
  - target: { kind: ConfigMap, name: loki-operations }   # ← add
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: loki-operations
        labels: { grafana_dashboard: "1" }
        annotations: { grafana_folder: Logs }

